<%= form_for(:todo, as: :todo, url:, html: { method: }) do |f| %>
  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description, autofocus: true, value: description %>
  </p>

  <p>
    <%= f.submit action_name, style: 'width: 80px' %>

    <%= link_to('Cancel', :back, style: 'margin-left: 10px') %>
  </p>
<% end %>
